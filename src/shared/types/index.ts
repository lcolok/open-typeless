/**
 * Shared type definitions.
 * Re-exports all types for convenient importing.
 */

export type {
  ASRConfig,
  ASRProvider,
  ASRResult,
  ASRStatus,
  AudioChunk,
} from './asr';

export type {
  AppSettings,
  AppSettingsUpdate,
  InteractionMode,
} from './settings';
